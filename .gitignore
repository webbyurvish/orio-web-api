# .NET / Visual Studio
bin/
obj/
.vs/

*.user
*.suo
*.cache
*.log

# Rider / JetBrains
.idea/

# VS Code
.vscode/

# ASP.NET Core secrets / local overrides
appsettings.*.Local.json
# Note: appsettings.*.Development.json does not match appsettings.Development.json
appsettings.Development.json
appsettings.*.Development.json

# Local env (never commit secrets)
.env
.env.local
.env.*.local

# Publish outputs
publish/
artifacts/
TestResults/

# Accidental local publish / compile-check output (never commit)
bin-compile-check/

# Runtime ASP.NET Data Protection keys (per environment)
data-protection-keys/

# Azure publish profiles (contain credentials)
*.PublishSettings
